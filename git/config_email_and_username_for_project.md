# config username and email for project
## use git config 
git config user.name "username"
git config user.email "xxx@xxx"
## edit the .git/config
add 
```
[user]
	user = username
	email = xxx@xxx
```
